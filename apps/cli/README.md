# CLI application

Terminal UI and commands live here. It should depend on public packages rather
than containing agent-loop behavior. The first implementation currently starts
in `src/cli.ts` and will move here when the loop milestone begins.

